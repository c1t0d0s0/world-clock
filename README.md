# World Clock (世界時計)

世界各地の都市の現在時刻をアナログ時計で一覧表示する、シンプルでスタイリッシュなダークテーマのWebアプリケーションです。

## 主な機能

- **リアルタイム アナログ時計**
  - 各都市の現地時間（時・分・秒）をリアルタイムで秒単位更新します。
- **午前/午後（AM/PM）の背景色切り替え**
  - 午前（AM）と午後（PM）で時計の盤面背景色が自動的に切り替わり、各都市が現在昼なのか夜なのかを一目で把握できます。
    - AM: 明るめのスレートブルー (`#54697e`)
    - PM: 深みのあるダークネイビー (`#1a2531`)
- **夏時間（サマータイム / DST）自動判定**
  - 各タイムゾーンにおけるサマータイムの適用状況を自動判定し、実施中の都市には都市名の横に `*` マークを表示します。
- **レスポンシブ対応**
  - PC、タブレット、スマートフォンの画面サイズに応じて、最適な時計サイズとグリッドレイアウト（3列 / 2列 / 1列）へ自動調整されます。

## 表示都市・タイムゾーン

| 配置 | 都市名 | タイムゾーン (IANA) |
| :--- | :--- | :--- |
| **Row 1** | Tokyo | `Asia/Tokyo` |
| | Madrid | `Europe/Madrid` |
| | London | `Europe/London` |
| **Row 2** | UTC | `UTC` |
| **Row 3** | New York | `America/New_York` |
| | Los Angeles | `America/Los_Angeles` |
| | Honolulu | `Pacific/Honolulu` |

## ファイル構成

```text
.
├── index.html     # アプリケーションの骨格・マークアップ
├── style.css      # スタイル定義（アナログ時計の描画・レイアウト・配色・メディアクエリ）
├── script.js      # 時刻取得・針の回転角度計算・AM/PM判定・DST判定
├── config.js      # 外部連携用設定（GTM ID等のオプショナル設定）
├── LICENSE        # ライセンスファイル (MIT License)
└── README.md      # 本ドキュメント
```

## 使い方

外部ライブラリへの依存やビルド手順は不要です。

### 1. ブラウザで直接開く
`index.html` をお好みのブラウザ（Google Chrome、Safari、Firefox、Edge 等）で直接開くだけで動作します。

### 2. ローカルサーバーを起動して開く
ローカルHTTPサーバーを利用する場合は、本ディレクトリで以下のコマンドを実行してください。

```bash
# Python 3 を利用する場合
python3 -m http.server 8000
```

起動後、ブラウザで `http://localhost:8000` にアクセスしてください。

## 補足・設定
- GTM (Google Tag Manager) を利用する場合は、`config.js` に `const GTM_ID = 'GTM-XXXXXXX';` を記述して設定できます。

## ライセンス
このプロジェクトは [MIT License](LICENSE) のもとで公開されています。
